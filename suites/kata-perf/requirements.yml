suite: kata-perf
requires:
  infrastructure:
    provider: aks
    bicep:
      template: infra/aks/main.bicep
      parameters: suites/kata-perf/infra.bicepparam
  kubernetes:
    minVersion: "1.36"
  nodeSelectors:
    - name: azurelinux-workload
      required: true
      minNodes: 1
      labels:
        perf.azure.com/node-role: workload
        kubernetes.azure.com/os-sku: AzureLinux
  observability:
    prometheus:
      required: true
      install: true
      namespace: perf-monitoring
      imageKey: prometheus
      serviceName: prometheus
      servicePort: 9090
      localPort: 9090
      requiredMetrics:
        - container_cpu_usage_seconds_total
        - container_memory_working_set_bytes
