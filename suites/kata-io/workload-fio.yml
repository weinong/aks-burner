{{- $profiles := list (dict "name" "randread-4k" "short" "rr") (dict "name" "randwrite-4k" "short" "rw") (dict "name" "seqread" "short" "sr") (dict "name" "seqwrite" "short" "sw") (dict "name" "fsync-heavy" "short" "fs") -}}
{{- $concurrencies := list 1 10 -}}
{{- $runtimes := list (dict "name" "standard" "short" "std") (dict "name" "kata" "short" "kata") -}}
{{- $storages := list (dict "name" "emptydir" "short" "ed" "template" "emptydir") (dict "name" "azure-disk" "short" "disk" "template" "pvc") -}}
jobs:
- name: kio-clean-jobs
  jobType: delete
  namespace: kata-io
  namespacedIterations: false
  skipIndexing: true
  preLoadImages: false
  qps: 20
  burst: 20
  objects:
  - kind: Job
    apiVersion: batch/v1
    labelSelector:
      app: kata-io
      benchmark: io
- name: kio-clean-pods
  jobType: delete
  namespace: kata-io
  namespacedIterations: false
  skipIndexing: true
  preLoadImages: false
  qps: 20
  burst: 20
  objects:
  - kind: Pod
    apiVersion: v1
    labelSelector:
      app: kata-io
      benchmark: io
- name: kio-clean-work-pvcs
  jobType: delete
  namespace: kata-io
  namespacedIterations: false
  skipIndexing: true
  preLoadImages: false
  qps: 20
  burst: 20
  objects:
  - kind: PersistentVolumeClaim
    apiVersion: v1
    labelSelector:
      app: kata-io
      benchmark: io
      pvc-role: work
- name: kio-preload-images
  jobType: create
  namespace: kata-io
  namespacedIterations: false
  jobIterations: 1
  qps: 1
  burst: 1
  cleanup: true
  waitWhenFinished: true
  maxWaitTimeout: 10m
  preLoadImages: true
  skipIndexing: true
  objects:
  - objectTemplate: templates/preload-pod.yml
    replicas: 1
    inputVars:
      jobName: '{{.k8sRunID}}-preload-workload'
  - objectTemplate: templates/preload-patch-pod.yml
    replicas: 1
    inputVars:
      jobName: '{{.k8sRunID}}-preload-patchpool'
{{ range $runtime := $runtimes -}}
{{ range $storage := $storages -}}
{{ range $concurrency := $concurrencies -}}
{{ range $profile := $profiles }}
- name: kio-{{ $runtime.short }}-{{ $storage.short }}-{{ $profile.short }}-{{ $concurrency }}
  jobType: create
  namespace: kata-io
  namespacedIterations: false
  jobIterations: {{ $concurrency }}
  qps: {{ $concurrency }}
  burst: {{ $concurrency }}
  cleanup: true
  waitWhenFinished: true
  preLoadImages: false
  objects:
{{- if eq $storage.name "azure-disk" }}
  - objectTemplate: templates/work-pvc.yml
    replicas: 1
    inputVars:
      jobName: '{{ $.k8sRunID }}-{{ $runtime.short }}-{{ $storage.short }}-{{ $profile.short }}-{{ $concurrency }}'
      storageType: storage-{{ $storage.name }}
      workStorageClass: managed-csi
      workAccessMode: ReadWriteOnce
{{- end }}
  - objectTemplate: templates/fio-{{ $storage.template }}-{{ $runtime.name }}-job.yml
    replicas: 1
    inputVars:
      jobName: '{{ $.k8sRunID }}-{{ $runtime.short }}-{{ $storage.short }}-{{ $profile.short }}-{{ $concurrency }}'
      scenario: runtime-{{ $runtime.name }}-storage-{{ $storage.name }}-fio-{{ $profile.name }}-concurrency-{{ $concurrency }}
      runtime: runtime-{{ $runtime.name }}
      storageType: storage-{{ $storage.name }}
      workloadType: fio
      fioProfile: /profiles/{{ $profile.name }}.fio
      fioProfileName: {{ $profile.name }}
      concurrency: '{{ $concurrency }}'
{{ end -}}
{{ end -}}
{{ end -}}
{{ end -}}
{{ range $concurrency := $concurrencies -}}
{{ range $profile := $profiles }}
- name: kio-kp-block-{{ $profile.short }}-{{ $concurrency }}
  jobType: create
  namespace: kata-io
  namespacedIterations: false
  jobIterations: {{ $concurrency }}
  qps: {{ $concurrency }}
  burst: {{ $concurrency }}
  cleanup: true
  waitWhenFinished: true
  preLoadImages: false
  objects:
  - objectTemplate: templates/work-block-pvc.yml
    replicas: 1
    inputVars:
      jobName: '{{ $.k8sRunID }}-kp-bl-{{ $profile.short }}-{{ $concurrency }}'
      storageType: storage-azure-disk-block
      workStorageClass: managed-csi
  - objectTemplate: templates/fio-block-kata-job.yml
    replicas: 1
    inputVars:
      jobName: '{{ $.k8sRunID }}-kp-bl-{{ $profile.short }}-{{ $concurrency }}'
      scenario: runtime-kata-patched-storage-azure-disk-block-fio-{{ $profile.name }}-concurrency-{{ $concurrency }}
      runtime: runtime-kata-patched
      storageType: storage-azure-disk-block
      workloadType: fio
      fioProfile: /profiles/{{ $profile.name }}.fio
      fioProfileName: {{ $profile.name }}
      concurrency: '{{ $concurrency }}'
{{ end -}}
{{ end -}}
